.next
node_modules
pnpm-lock.yaml
*.md
.vscode
public

# `next build` rewrites tsconfig.json (mandatory jsx: react-jsx + its own
# array formatting). Let Next own this file so Prettier doesn't fight it.
tsconfig.json
# drizzle-kit owns its generated migration artifacts
src/lib/db/migrations
